name: CI

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v3

      - name: create a new tag
        run: |
          git config --local user.email "keystonebarreto@gmail.com"
          git config --local user.name "Keystone"
          git tag -a build-${{ github.run_number }} -m "Versão ${{ github.run_number }}"
          git push origin --tags
